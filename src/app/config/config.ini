# because the trust anchor won't always be running on this box, can't necessarily reach into its agent configuration
[Trust Anchor]
host=trust-anchor
port=${HOST_PORT_TRUST_ANCHOR}

# Node pool
[Pool]
# genesis.txn.path=${HOME}/src/app/config/bootstrap/genesis.txn
